import 'package:flutter/material.dart';

class FuchsiaPage extends StatelessWidget {
  const FuchsiaPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FuchsiaPage'),
      ),
    );
  }
}
