import 'package:flutter/material.dart';

class LinuxPage extends StatelessWidget {
  const LinuxPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LinuxPage'),
      ),
    );
  }
}
