import 'package:flutter/material.dart';

class WindowsPage extends StatelessWidget {
  const WindowsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WindowsPage'),
      ),
    );
  }
}
